#!/usr/bin/env bash
# Convenience wrapper so you can run ./tg <command> without activating the venv.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/.venv/bin/python" "$DIR/tg.py" "$@"
